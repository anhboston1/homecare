import { ProjectlistPipe } from './projectlist.pipe';

describe('ProjectlistPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectlistPipe();
    expect(pipe).toBeTruthy();
  });
});
