import { PostprojectPipe } from './postproject.pipe';

describe('PostprojectPipe', () => {
  it('create an instance', () => {
    const pipe = new PostprojectPipe();
    expect(pipe).toBeTruthy();
  });
});
