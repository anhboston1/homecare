import { BidprojectPipe } from './bidproject.pipe';

describe('BidprojectPipe', () => {
  it('create an instance', () => {
    const pipe = new BidprojectPipe();
    expect(pipe).toBeTruthy();
  });
});
